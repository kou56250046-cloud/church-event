# 要件定義書:教会イベント企画カタログ

| 項目 | 内容 |
|---|---|
| ドキュメント名 | 教会イベント企画カタログ 要件定義書 |
| バージョン | 1.1(詳細ページのHTML対応・デザイン方針変更を反映) |
| 作成日 | 2026-06-11 |
| ステータス | ドラフト(発注者レビュー待ち) |

---

## 1. 背景と目的

### 1.1 背景
教会でイベントを開催するたびに「何をするか・どのような内容にするか」をゼロから企画しており、毎回の検討に大きな手間がかかっている。

### 1.2 目的
過去のアイデア・実績を蓄積した**イベント企画カタログ**を構築し、以下を実現する。

- カテゴリーから種目を選ぶだけで企画の叩き台が決まる状態をつくる
- イベントの詳細企画書(Claude Code等で生成したMarkdown)を一元管理し、見栄えの良い形で閲覧できるようにする
- 開催履歴・反省メモを記録し、次回以降の企画に活かせるようにする

### 1.3 ゴール(成功基準)
- イベント企画の検討開始から「種目決定+詳細企画書の確認」までを**5分以内**で行える
- 完全無料で運用でき、月額・従量課金が一切発生しない
- PC・スマホのどちらからでも同じデータを閲覧・編集できる

---

## 2. 利用者・利用環境

| 項目 | 内容 |
|---|---|
| 利用者 | 企画者本人のみ(1名)。一般公開・メンバー共有は行わない |
| 利用端末 | PCおよびスマートフォンの両方 |
| ブラウザ | モダンブラウザ(Chrome / Safari / Edge の最新版)を想定 |
| 利用頻度 | イベント企画時(月数回程度)+履歴記録(イベント開催後) |

---

## 3. システム方式(技術構成)

### 3.1 全体構成
**「GitHub Pages + 静的SPA + GitHubリポジトリをデータベース代わりに使う」構成**とする。サーバー・データベースを一切持たないため、恒久的に無料で運用できる。

```
[ブラウザ (PC / スマホ)]
   │  閲覧・検索・編集UI(index.html / SPA)
   │
   ├─ 読み込み: GitHub Contents API (GET) で events.json を取得
   └─ 保存:     GitHub Contents API (PUT) で events.json を更新
                      │
              [GitHubリポジトリ]
               ├─ index.html      … アプリ本体(1ファイル)
               ├─ data/events.json … 全イベントデータ(カテゴリー・詳細・履歴)
               └─ GitHub Pages でホスティング(無料)
```

### 3.2 技術選定と理由

| 項目 | 採用技術 | 理由 |
|---|---|---|
| ホスティング | GitHub Pages | 無料・個人運用・静的サイトの定番。独自ドメイン不要 |
| データ保存 | リポジトリ内の `events.json` | PC・スマホ間で同期可能。Git履歴が自動バックアップになる |
| 書き込み手段 | GitHub Contents API + Fine-grained PAT(個人アクセストークン) | ブラウザだけで保存が完結。サーバー不要 |
| フロントエンド | 素のHTML/CSS/JavaScript(1ファイルSPA) | ビルド不要、メンテが容易。CDNからmarked.js(Markdown描画)のみ利用 |
| 詳細企画書の描画 | HTML:サンドボックス化iframe(srcdoc)に直接描画 / Markdown:marked.jsで変換 | Claude Code生成のデザイン付きHTMLを、アプリ本体と干渉させずそのままの見た目で表示できる |
| 詳細企画書の生成 | Claude Code(チャットでHTMLまたはMarkdownを生成) | 生成結果を管理画面(編集フォーム)に貼り付けるだけで反映。見栄え重視の場合はHTML推奨 |

### 3.3 認証・セキュリティ方針
- リポジトリは**プライベートリポジトリ**を推奨(個人メモを含むため)
  - ※プライベートリポジトリではGitHub Pagesが使えない(無料プラン)ため、その場合アプリは「ローカルにindex.htmlを置いて開く」または「公開リポジトリ(データは閲覧されうる)」のいずれかを選択する。**推奨構成は §3.4 参照**
- 書き込み用トークンは **Fine-grained PAT(対象リポジトリ1つ・Contents権限のみ・有効期限付き)** とし、ブラウザのlocalStorageに保存する
- トークンをHTMLやJSONにハードコードしない

### 3.4 推奨リポジトリ構成(2案)

| 案 | 構成 | メリット | デメリット |
|---|---|---|---|
| **A案(推奨)** | 公開リポジトリ1つに index.html + data を置き、GitHub Pagesで配信 | URLにアクセスするだけで使える。スマホ運用が最も簡単 | データ(企画メモ)が理論上は誰でも閲覧可能(URLを知らなければ実害は小さい) |
| B案 | プライベートリポジトリにdataを置き、index.htmlは各端末にローカル保存 | データ非公開 | スマホでのローカルHTML運用がやや煩雑 |

→ 個人名・住所等の機微情報をデータに書かない運用とし、**A案を第一候補**とする。最終決定は実装前にユーザー確認。

---

## 4. 機能要件

### 4.1 機能一覧

| ID | 機能名 | 優先度 | 概要 |
|---|---|---|---|
| F-01 | カテゴリー別閲覧 | 必須 | カテゴリーを選ぶと所属イベント一覧をカード表示 |
| F-02 | キーワード検索 | 必須 | タイトル・概要・タグ・詳細本文を横断検索 |
| F-03 | 絞り込みフィルタ | 必須 | 人数・季節・予算で絞り込み(複合条件可) |
| F-04 | イベント詳細表示 | 必須 | Markdownの企画書をデザインされたページとして表示 |
| F-05 | イベント追加・編集 | 必須 | ブラウザ上のフォームで追加・編集し、GitHubへ保存 |
| F-06 | 開催履歴・メモ記録 | 必須 | イベントごとに「開催日+メモ」を複数件記録・閲覧 |
| F-07 | カテゴリー管理 | 必須 | カテゴリーの追加(名前・アイコン・テーマ色) |
| F-08 | GitHub接続設定 | 必須 | リポジトリ情報・トークンの設定、接続テスト |
| F-09 | イベント削除 | 必須 | 確認ダイアログ付きで削除 |
| F-10 | データエクスポート | 推奨 | events.jsonを手動ダウンロード(予備バックアップ) |
| F-11 | 閲覧専用フォールバック | 推奨 | トークン未設定時も閲覧・検索だけは可能 |

※ ヒヤリングで不要と確認した機能:ランダム提案(ガチャ)機能 → **対象外**(将来拡張候補として §8 に記載)

### 4.2 主要機能の詳細

#### F-02 キーワード検索
- 検索対象:イベント名 / 概要 / タグ / 詳細企画書本文(Markdown)
- 部分一致・大文字小文字無視。スペース区切りでAND検索
- 入力と同時に結果を絞り込む(インクリメンタルサーチ)

#### F-03 絞り込みフィルタ
| 軸 | 選択肢 |
|---|---|
| 人数 | 〜10人 / 10〜30人 / 30人〜(イベント側は最小〜最大人数で保持し、範囲が重なれば一致) |
| 季節 | 春 / 夏 / 秋 / 冬 / 通年(複数選択可) |
| 予算 | 無料 / 〜500円/人 / 〜1,500円/人 / 1,500円〜/人 |

- カテゴリー・キーワードとの組み合わせ(AND条件)が可能
- 適用中の条件はチップ表示し、ワンタップで解除できる

#### F-04 イベント詳細表示(HTML / Markdown 両対応)
- 詳細企画書は **HTML形式とMarkdown形式の両方に対応**し、貼り付けた内容から自動判定する(`<` で始まる/HTMLタグを含む → HTML、それ以外 → Markdown)
- **HTML**:サンドボックス化したiframe(srcdoc)内に描画する。企画書自身が持つCSS・配色・レイアウトがアプリ本体と干渉せず、Claude Codeが生成したデザインそのままで表示される(見栄え重視の推奨形式)
- **Markdown**:marked.jsで変換し、見出し・表・リストが映えるアプリ標準スタイルで表示する(手早く書きたいとき用)
- 基本属性(人数・季節・予算・タグ)をバッジ表示
- 同画面から「編集」「開催履歴の追加」へ遷移できる

#### F-05 イベント追加・編集(管理画面)
- 編集は管理画面(編集フォーム)から行う
- 入力項目:イベント名(必須)/ カテゴリー(必須)/ 概要 / 最小・最大人数 / 季節(複数)/ 予算区分 / タグ / 詳細企画書(**HTMLまたはMarkdown**を貼り付け)
- 詳細企画書入力欄は「編集 ⇔ プレビュー」を切替可能(プレビューは実際の詳細表示と同じ描画方式)
- 保存時はGitHubから最新のshaを取得してからPUTする(更新競合の基本対策)
- 保存成功・失敗を明示的に通知する

#### F-06 開催履歴・メモ
- 1イベントに対し複数件の履歴(開催日 / 自由記述メモ)を記録
- 詳細画面に新しい順で一覧表示
- 一覧カードに「開催回数」「最終開催日」をバッジ表示し、企画選定の参考にする

#### F-08 GitHub接続設定
- 設定項目:ユーザー名(owner)/ リポジトリ名 / ブランチ(既定 main)/ データファイルパス(既定 `data/events.json`)/ Fine-grained PAT
- 設定はlocalStorageに保存(端末ごとに初回1回の設定)
- 「接続テスト」ボタンで読み書き権限を検証できる

---

## 5. データ要件

### 5.1 データモデル(events.json)

```json
{
  "version": 1,
  "categories": [
    { "id": "outdoor", "name": "アウトドア", "icon": "⛰️", "color": "#3E7C59" }
  ],
  "events": [
    {
      "id": "river-play",
      "category": "outdoor",
      "title": "川遊び",
      "summary": "夏の定番。浅瀬での水遊びとスイカ割り。",
      "peopleMin": 10,
      "peopleMax": 40,
      "seasons": ["夏"],
      "budget": "low",
      "tags": ["子ども向け", "ファミリー"],
      "detailFormat": "html",
      "detail": "<!DOCTYPE html><html>…(HTML企画書全文。Markdownの場合は \"detailFormat\": \"markdown\")…</html>",
      "history": [
        { "date": "2025-07-20", "memo": "参加32名。ライフジャケット増備が必要。" }
      ],
      "createdAt": "2026-06-11",
      "updatedAt": "2026-06-11"
    }
  ]
}
```

- `budget` は `free / low / mid / high` の4区分(表示名は §4.2 F-03 のとおり)
- `detailFormat` は `html / markdown` の2区分。保存時に内容から自動判定し、手動で切替も可能とする
- IDは英数字スラッグ(自動生成可)。Git履歴がそのまま変更履歴・バックアップとなる

### 5.2 初期データ(カテゴリー・イベント案)

| カテゴリー | 初期イベント案 |
|---|---|
| ⛰️ アウトドア | 川遊び、登山、ハイキング、デイキャンプ、星空観察会 |
| 🏃 スポーツ | 運動会、モルック大会、バドミントン大会、ドッジボール大会、ボウリング大会 |
| 🎲 室内 | ボードゲーム会、お菓子作り、スマブラ大会、人狼ゲーム、カラオケ大会 |
| 🎄 季節・行事 | クリスマス会、イースターエッグハント、餅つき大会、流しそうめん、芋煮会 |
| 🍞 食・交流 | BBQ、ポットラックパーティー、国際料理デー、パン作り教室 |
| 📖 学び・文化 | 読書会、映画鑑賞会、聖書クイズ大会、ゴスペルワークショップ、写真散歩 |
| 🤝 奉仕・地域 | 地域清掃、フードドライブ、施設訪問、子ども食堂のお手伝い |

- 初期データには各イベントの概要・人数目安・季節・予算区分を設定する
- 代表3件程度(例:川遊び / モルック大会 / クリスマス会)には、Claude Code生成を想定した**フル詳細企画書のサンプル(HTML形式2件+Markdown形式1件)**を同梱し、両形式の運用イメージと見栄えの違いを確認できるようにする

---

## 6. 非機能要件

| 分類 | 要件 |
|---|---|
| コスト | 初期費用・運用費用ともに0円(GitHub無料枠の範囲内) |
| 対応端末 | レスポンシブ対応(スマホ幅 約375px〜PCワイドまで)。スマホでの片手操作を考慮 |
| 性能 | イベント数 数百件規模まで体感遅延なく検索・絞り込みできること(クライアントサイド処理) |
| 可用性 | GitHub/GitHub Pagesの稼働に準拠。障害時もJSONエクスポートでデータ持ち出し可能 |
| セキュリティ | トークンは最小権限・期限付き。コードやデータファイルに認証情報を含めない |
| バックアップ | Gitコミット履歴による自動世代管理+手動JSONエクスポート(F-10) |
| 保守性 | index.html 1ファイル構成。ビルドツール・パッケージ管理を使わない |
| デザイン | ライトグリーン基調の明るく爽やかな配色(新緑・若葉のイメージ)。カテゴリーごとに明るいテーマ色を割り当て、温かみと軽やかさを両立する |
| アニメーション | 動的でおしゃれな演出を実装する:①ページ表示時のステッガード(時間差)フェードイン ②スクロールに応じたカードの出現アニメーション ③カードホバー時の浮き上がり・色彩演出 ④画面遷移のスムーズなトランジション ⑤検索・絞り込み結果の滑らかな入れ替わり。ただし操作の邪魔をしない速度(0.2〜0.6秒程度)とし、OSの「視差効果を減らす」設定(prefers-reduced-motion)を尊重する |

---

## 7. 運用フロー

### 7.1 初期セットアップ(初回のみ・約15分)
1. GitHubリポジトリを作成し、`index.html` と `data/events.json` を配置
2. GitHub Pagesを有効化
3. Fine-grained PAT(対象リポジトリのContents: Read and write のみ)を発行
4. サイトを開き、設定画面でリポジトリ情報とトークンを入力 → 接続テスト
5. スマホでも同URLを開き、同様にトークンを設定(ホーム画面に追加推奨)

### 7.2 日常運用
- **企画するとき**:カテゴリー/検索/絞り込みで候補を探す → 詳細企画書を確認
- **新しいアイデアを得たとき**:Claude Codeに「◯◯の企画書を、デザイン込みの単一HTMLファイルで」と依頼(手早く済ませたいときはMarkdownでも可)→ 生成結果を管理画面の編集フォームに貼り付けて保存
- **イベント開催後**:該当イベントの詳細画面から開催日とメモを記録

---

## 8. 対象外事項・将来拡張候補

### 8.1 今回の対象外(スコープ外)
- 複数ユーザーでの共同編集、ログイン機能
- 教会メンバーへの公開・告知ページ機能
- 画像アップロード(必要な場合はリポジトリへ直接追加し、Markdownから参照する運用で代替)
- オフライン編集(閲覧キャッシュは可、編集はオンライン前提)

### 8.2 将来拡張候補
- ランダム提案(ガチャ)機能
- 「最近やっていない順」ソート、季節のおすすめ表示
- 告知資料(チラシ・案内文)生成との連携
- 履歴データの年間振り返りレポート

---

## 9. 成果物一覧

| # | 成果物 | 形式 |
|---|---|---|
| 1 | アプリ本体 | `index.html`(単一ファイルSPA) |
| 2 | 初期データ | `data/events.json`(カテゴリー7種+イベント約30件+詳細サンプル:HTML企画書2件・Markdown企画書1件) |
| 3 | セットアップ手順書 | `README.md`(リポジトリ作成〜トークン設定までの手順) |

---

## 10. 未決事項(実装前に要確認)

| # | 項目 | 選択肢 | 推奨 |
|---|---|---|---|
| 1 | リポジトリ公開設定 | A案:公開+GitHub Pages / B案:非公開+ローカルHTML | A案(§3.4) |
| 2 | サイト名 | 例:「イベントの引き出し」「企画カタログ」など | 実装時に確認 |
| 3 | 初期データの予算・人数の目安値 | 叩き台を実装側で設定 → 利用しながら修正 | 叩き台で開始 |
